################################################################################
# Automatically-generated file. Do not edit!
################################################################################

-include ../makefile.init

RM := rm -rf

# All of the sources participating in the build are defined here
-include sources.mk
-include subdir.mk
-include src/subdir.mk
-include objects.mk

ifneq ($(MAKECMDGOALS),clean)
ifneq ($(strip $(C_DEPS)),)
-include $(C_DEPS)
endif
ifneq ($(strip $(S_UPPER_DEPS)),)
-include $(S_UPPER_DEPS)
endif
endif

-include ../makefile.defs

# Add inputs and outputs from these tool invocations to the build variables 
ELFSIZE += \
empty_application_testtt.elf.size \

ELFCHECK += \
empty_application_testtt.elf.elfcheck \


# All Target
all: empty_application_testtt.elf secondary-outputs

# Tool invocations
empty_application_testtt.elf: $(OBJS) ../src/raw_apps.ld $(USER_OBJS)
	@echo Building target: $@
	@echo Invoking: MicroBlaze gcc linker
	mb-gcc -Wl,-T -Wl,../src/raw_apps.ld -L../../standalone_bsp_lwip_xilmfs/microblaze_0/lib -mlittle-endian -mxl-barrel-shift -mxl-pattern-compare -mno-xl-soft-div -mcpu=v8.20.a -mno-xl-soft-mul -mxl-multiply-high -mhard-float -mxl-float-convert -mxl-float-sqrt -o"empty_application_testtt.elf" $(OBJS) $(USER_OBJS) $(LIBS)
	@echo Finished building target: $@
	@echo ' '

empty_application_testtt.elf.size: empty_application_testtt.elf
	@echo Invoking: MicroBlaze Print Size
	mb-size empty_application_testtt.elf  |tee "empty_application_testtt.elf.size"
	@echo Finished building: $@
	@echo ' '

empty_application_testtt.elf.elfcheck: empty_application_testtt.elf
	@echo Invoking: Xilinx ELF Check
	elfcheck empty_application_testtt.elf -hw ../../hw_platform/system.xml -pe microblaze_0  |tee "empty_application_testtt.elf.elfcheck"
	@echo Finished building: $@
	@echo ' '

# Other Targets
clean:
	-$(RM) $(OBJS)$(C_DEPS)$(EXECUTABLES)$(ELFSIZE)$(ELFCHECK)$(S_UPPER_DEPS) empty_application_testtt.elf
	-@echo ' '

secondary-outputs: $(ELFSIZE) $(ELFCHECK)

.PHONY: all clean dependents
.SECONDARY:

-include ../makefile.targets
